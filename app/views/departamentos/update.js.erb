<% if @product.errors.empty? %>
  // add the row to the table, show alert and hide modal
  $('table tbody tr#<%= @departamento.id %>').replaceWith(' <%= j render "row", departamento: @departamento %>');
  $('.alert-success').html("El departamento ha sido modificado con éxito").show();
  $('#departamento-modal').modal('hide');
<% else %>
  // show first error
  alert('<%= @departamento.errors.full_messages[0] %>');
<% end %>