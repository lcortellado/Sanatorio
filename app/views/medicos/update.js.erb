<% if @medico.errors.empty? %>
  // add the row to the table, show alert and hide modal
  $('table tbody tr#<%= @medico.id %>').replaceWith(' <%= j render "row", medico: @medico %>');
  $('.alert-success').html("La Medico ha sido modificado con éxito").show();
  $('#medico-modal').modal('hide');
<% else %>
  // show first error
  alert('<%= @medico.errors.full_messages[0] %>');
<% end %>