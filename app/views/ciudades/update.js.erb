<% if @ciudade.errors.empty? %>
  // add the row to the table, show alert and hide modal
  $('table tbody tr#<%= @ciudade.id %>').replaceWith(' <%= j render "row", ciudade: @ciudade %>');
  $('.alert-success').html("La Ciudad ha sido modificado con éxito").show();
  $('#ciudade-modal').modal('hide');
<% else %>
  // show first error
  alert('<%= @ciudade.errors.full_messages[0] %>');
<% end %>