<% if @paciente.errors.empty? %>
  // add the row to the table, show alert and hide modal
  $('table tbody tr#<%= @paciente.id %>').replaceWith(' <%= j render "row", paciente: @paciente %>');
  $('.alert-success').html("Paciente ha sido modificado con éxito").show();
  $('#paciente-modal').modal('hide');
<% else %>
  // show first error
  alert('<%= @paciente.errors.full_messages[0] %>');
<% end %>