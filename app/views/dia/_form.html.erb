<%= form_with(model: dias, local: true) do |form| %>
  <% if dias.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dias.errors.count, "error") %> prohibited this dias from being saved:</h2>

      <ul>
      <% dias.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :codigo %>
    <%= form.text_field :codigo, id: :dias_codigo %>
  </div>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre, id: :dias_nombre %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
