<%= form_with(model: distrito, local: true) do |form| %>
  <% if distrito.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(distrito.errors.count, "error") %> prohibited this distrito from being saved:</h2>

      <ul>
      <% distrito.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre, id: :distrito_nombre %>
  </div>

  <div class="field">
    <%= form.label :departamento_id %>
    <%= form.text_field :departamento_id, id: :distrito_departamento_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
