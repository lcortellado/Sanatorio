<% if @ciudade.errors.empty? %>
  // add the row to the table, show alert and hide modal
  $('table tbody tr#<%= @insumo.id %>').replaceWith(' <%= j render "row", insumo: @insumo %>');
  $('.alert-success').html("Insumo  ha sido modificado con éxito").show();
  $('#insumo-modal').modal('hide');
<% else %>
  // show first error
  alert('<%= @insumo.errors.full_messages[0] %>');
<% end %>