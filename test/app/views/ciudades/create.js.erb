<% if @ciudade.errors.empty? %>
  // add the row to the table, show alert and hide modal
  $('table tbody').append(' <%= j render "row", ciudade: @ciudade %>');
  $('.alert-success').html("La ciudad ha sido creado con éxito").show();
  $('#ciudade-modal').modal('hide');
<% else %>
  // show first error
  alert('<%= @ciudade.errors.full_messages[0] %>');
<% end %>