<p id="notice"><%= notice %></p>

<h1>Pedidos</h1>

<table class="table table-striped">
  <thead>
    <tr>
      <th>Codigo</th>
      <th>Fecha</th>
      <th>Proveedore</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @pedidos.each do |pedido| %>
      <tr>
        <td><%= pedido.codigo %></td>
        <td><%= pedido.fecha %></td>
        <td><%= pedido.proveedore.nombre %><%= pedido.proveedore.apellido %></td>
        <td><%= link_to 'Show', pedido %></td>
        <td><%= link_to 'Edit', edit_pedido_path(pedido) %></td>
        <td><%= link_to 'Destroy', pedido, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Pedido', new_pedido_path %>
